<h4 class="text-center bg-black parent-title py-3 fw-bold">
  <%= @child.full_name %>
</h4>

<section class="container bg-white rounded-4 shadow p-3 mt-0 mt-md-3">
  <%= render "parents/children/components/information-card", child: @child, start_year: @start_year, membership: @membership %>
</section>

<section class="container my-3 shadow p-0 rounded-4 bg-white">
  <h4 class=" text-center p-3 bg-black text-white rounded-top-4">
    Adhésion
  </h4>

  <div class="bg-white rounded-bottom-4 p-3">
    <%= render "parents/children/components/memberships-card", memberships: @memberships %>
  </div>
</section>

<section class="container my-3 shadow p-0 rounded-4 bg-white">
    <h4 class=" text-center p-3 bg-black text-white rounded-top-4">
      Stages
    </h4>

    <div class="bg-white rounded-bottom-4 p-3">
      <%= render "parents/children/components/camp-enrollments-card", camp_enrollments: @camp_enrollments %>
    </div>
</section>

<section class="container my-3 shadow p-0 rounded-4 bg-white">
  <h4 class="text-center p-3 bg-black text-white rounded-top-4">
    Programmes annuels
  </h4>
  <div class="bg-white rounded-bottom-4 p-3">
    <%= render "parents/children/components/annual-programs-card", annual_program_enrollments: @annual_program_enrollments %>
  </div>
</section>
